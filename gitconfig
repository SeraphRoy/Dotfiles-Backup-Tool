[alias]
   pb = pull --rebase
   details = log -n1 -p --format=fuller
   co = checkout
   ci = commit
   st = status
   br = branch
   review = review -T
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
	unstage = reset HEAD
	undo-commit = reset --soft HEAD~1
   aliases = !git config --list | grep 'alias\\.' | sed 's/alias\\.\\([^=]*\\)=\\(.*\\)/\\1\\ \t => \\2/' | sort
   branches = branch -av
   brdel = branch -D
   rbi = rebase -i
   rbc = rebase --continue
[color]
    ui = true
[user]
	name = SeraphRoy
	email = royxagain@gmail.com
[remote "review-turbine"]
  url = ssh://gerrit:29418/arista
   push = HEAD:refs/for/master%r=alexliu,r=ryan,r=jwang,r=jqiu,r=alexyang,r=eseither,r=gsirton,r=tomas,r=rprada,r=cyding,r=lreyreaud,r=daireomuirgheasa,r=paveyry,r=apech,r=jcr,r=jenkins
